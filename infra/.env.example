# Video Platform - Production Environment Variables
# Copy this file to .env and update values before deployment

# --- Database ---
POSTGRES_USER=video_user
POSTGRES_PASSWORD=video_password
POSTGRES_DB=video_platform

# --- Security (CHANGE THESE IN PRODUCTION!) ---
JWT_SECRET=change-me-in-production-use-a-32-char-secret
REFRESH_TOKEN_SECRET=change-me-in-production-use-another-secret

# --- Frontend URL ---
FRONTEND_URL=http://localhost

# --- Optional: Override ports (defaults shown) ---
# BACKEND_PORT=4000
# FRONTEND_PORT=80
